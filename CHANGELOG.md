# 更新日志

此文件记录项目中所有值得关注的历史变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，
并遵循 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

本文件已于 2026-04-15 根据仓库提交历史与现有项目文档完成历史回填。

## [Unreleased]

## [0.1.1-015] - 2026-05-06 - 新增数据源配置入口、数据可信层等，并优化数据快照性能、补源证据文案等

### 新增 (Added)

- **数据源配置入口**: 数据快照页会在底部统一汇总缺少的补源配置，选择后展示对应本机输入和复制设置加重启命令入口，并提示必须在启动 QuickStart 的同一个 PowerShell 中执行，方便操作者补齐配置同时不持久化本机输入值。
- **数据可信层**: 数据快照页新增统一可信层摘要，按价格主链、成分股历史、退市身份、公司行动、长周期补丁和精修来源解释每类数据源能证明什么、不能证明什么。
- **PIT 补源优先级**: PIT 修复队列新增下一补源、所需证据、可信阻断原因和 zero-event 候选证书投影，帮助运维按 Tiingo、FMP、Stooq/Kaggle、SEC/CIK、Polygon 的顺序闭合 Full Ready 门禁。
- **因子后端治理**: 因子库新增统一诊断状态、阻断与风险提示口径，以及只读批量诊断预览摘要，帮助研究员区分稳健、待校准、失效和沙箱因子。

### 优化 (Changed)

- **数据快照性能**: 数据源登记、尝试记录与可信层摘要复用快照短缓存，减少重复打开数据快照或查看补源状态时的长等待。
- **补源证据文案**: 数据快照页的补源优先级模块改为中文金融表达，突出价格、成分、身份、公司行动和精修边界，避免 raw 枚举词干扰判断。
- **PIT 清洗中心文案**: PIT 清洗中心全局文案改为简洁中文金融表达，减少英文运维词和 raw 技术枚举对门禁判断的干扰。
- **数据源治理**: Tiingo、FMP、Stooq 和 SEC EDGAR 的角色文案改为证据边界优先，接入配置只展示可用性状态；Stooq 在线补丁保持手动开启、单标的缓存和 price-only 语义。
- **数据快照页**: 股票页可信层下沉到页面底部，并改为独立的补源优先级、证据边界和卡片样式表达，减少对主工作台的干扰。
- **因子库体验**: 系统默认因子说明、最近诊断列和沙箱诊断浮层改为更紧凑的研究台表达，减少后端枚举外露和表格阅读拥挤。
- **PIT 清洗中心**: 点时样本池历史锚点改为年度摘要展示，同时保留原始锚点数量说明，减少页面噪音并突出长期样本池趋势。
- **目录归位**: 恢复证据、恢复脚本、历史备份和本地日志按用途归入专门目录，并补充后续写入规范，减少主目录噪音。

### 修复 (Fixed)

- **因子库浮层**: 沙箱诊断摘要改为贴近所点击的状态按钮展示，末行向上展开，避免表格底部被浮层撑出空白。
- **数据源配置草稿**: 补源优先级模块的配置输入改为当前浏览器标签页暂存，刷新页面后仍可继续复制启动命令，并提供清空暂存入口。
- **数据快照页**: 标普500 股票池覆盖明细改为按半年度 PIT 锚点统计，忽略旧成员行里的日级变更日期，避免 61 个历史锚点被误显示成 2,753 个待补锚点。
- **PIT 外部补源状态**: PIT 清洗中心现在能识别嵌套在本地缓存根目录下的既有外部补源包，避免已有 Matrix、Kaggle catalog 和 Parquet 缓存被误报为 missing。
- **挖掘沙盒数据真实性**: 挖掘沙盒首屏任务队列与候选摘要改为读取运行时挖掘接口，创建任务必须使用真实价格快照，缺少快照时直接阻断，避免静态样例或 synthetic 数据进入页面判断。
- **挖掘任务提交**: 创建挖掘任务改为快速写入运行队列并由后台线程完成候选评估，页面创建按钮会防重复提交并轮询任务状态，避免 `/factor-mining/jobs` 长时间 pending。
- **挖掘任务进度**: 因子挖掘任务运行中会持续刷新已评估候选数和进度百分比，避免长任务在完成前一直显示为 0。
- **挖掘沙盒去重**: 任务队列和候选摘要会清理重复配置与重复表达式，创建前会提示已存在的相同任务，列表超过三条时改为模块内滚动查看。
- **因子诊断详情**: 因子详情页改为轻量读取首屏数据，并在后台补齐 PIT 版本信息，避免首次打开诊断报告时被完整数据治理概览拖慢。
- **因子库体验**: 因子库列表改为轻量门禁投影，修复普通本地页面加载失败、首屏加载过慢、阻断列文字溢出、英文新增因子名和无 IC 数据占位，同时移除资源队列模块并让相关性热力图与列表等宽。
- **因子库研究门禁**: Sandbox 研究态的 Verified PIT 窗口缺口改为风险提示而非硬阻断；新增因子缺少真实诊断时展示默认因子参考口径指标，列表阻断列和末行缺口弹层完整可读。
- **PIT 清洗中心**: 点时样本池历史锚点模块最多展示三行，超出内容可在模块内滚动查看，避免历史锚点过多时拉长页面。
- **本地启动器**: 后端慢启动时会延长健康等待并追加真实健康复核，同时将重读模型预热移到后台并去掉前端重复构建，避免服务实际就绪却被启动窗口误报失败，也减少本地启动等待。
- **临时产物收口**: backend 固定测试脚本的 pytest basetemp、Python 临时目录与 pytest cache 统一进入项目内 `.tmp` 运行目录，并补充 repo 级 pytest 门禁，避免测试和调试输出继续散落在项目主目录或项目外临时目录。
- **多因子行业中性化**: `SP500` 行业中性化改为读取 universe membership 的真实 GICS PIT 元数据，启用后可通过预览、创建和回测门禁；缺失行业 PIT 时仍保留 blocker，避免用静态行业兜底。
- **多因子预览性能**: 策略创建页预览接口的行业 PIT 读取改为按当前候选 symbol、as-of 日期和有效成员状态切片查询，避免百万级 universe membership 历史全量扫描导致 `/factor-models/preview` 不返回。

### 移除 (Removed)

- **PIT 清洗中心模块**: 移除 Full Ready 免费源修复队列、补源优先级与证据层、外部缓存与精修就绪度三个首屏模块，让页面聚焦 PIT 门禁、覆盖率下钻、清洗规则和身份修复。

## [0.1.1-014] - 2026-05-05 - 新增系统默认因子、挖掘沙盒等，并优化多因子现有链路增强、数据源治理等

### 新增 (Added)

- **系统默认因子**: 新增估值、动量、质量、低波和规模五类共七个默认因子，支持基于点时数据进行诊断和组合。
- **挖掘沙盒**: 新增公式因子挖掘任务视图，研究员可以查看候选表达式、运行进度、失败样本和风险提示。
- **多因子策略创建**: 新增策略库多因子创建入口，可选择因子、配置权重和行业中性化后创建可回测策略。
- **数据源治理**: 新增只读数据源登记与尝试记录接口，帮助查看供应商可用性、凭据状态、配额冷却和 PIT 权限。
- **PIT Full Ready 修复队列**: PIT 清洗中心新增免费源修复队列、供应商冷却提示和拒绝伪 Ready 规则，让研究员能看到距离正式可用还差哪些历史价格、公司行为和身份映射证据。
- **PIT 外部补源**: 新增 Matrix、Kaggle 批量缓存和 Polygon 精修的就绪度投影与本地执行入口，帮助数据运维按历史成员、退市行情和关键公司行动证据补齐 Full Ready 阻塞。

### 优化 (Changed)

- **多因子现有链路增强**: `MULTI_FACTOR` 策略接入既有策略详情、回测配置、回测详情、优化配置和优化结果链路，新增可选 profile/precheck/attribution/range 视图，保持原页面结构并用中文参数 formatter 避免 raw JSON 外泄。
- **数据源治理**: 数据源可用性摘要区分已登记、已启用、凭据就绪和当前可用，并按唯一供应商优先展示最新刷新收益，减少重复尝试记录造成的误判。
- **数据源治理**: Provider registry 增补外部 PIT 源的 URL、license、manifest、schema fingerprint 和凭据状态字段，凭据只暴露 present/missing/invalid，不返回密钥内容。
- **OpenBB 覆盖评估**: OpenBB 摘要补充凭据就绪、当前可用和最新刷新尝试计数，便于在配置只读密钥后按真实落地数据评估覆盖收益。
- **数据快照页**: 股票/指数快照的最新刷新卡片新增“查看明细”，弹层以中文表格展示当前覆盖、本次变化、待补缺口和主要来源。

### 修复 (Fixed)

- **多因子链路验收**: 多因子策略详情、回测预览和优化详情改走轻量读取与约束修复路径；回测配置页预检模块固定在现有配置模块下方，归因条按贡献差异展示，优化配置默认约束因子权重合计为 100%。
- **PIT 外部补源**: 差异导入现在优先按价格缺口执行，并能识别历史 ticker 与 share-class 的常见别名，避免把公司行动缺口误当作 Kaggle 价格补数处理。
- **PIT 外部补源**: 后续修复批次会保留已落地的外部价格补源目标池和 provider 证据，避免公司行动修复顺手扩宽价格 denominator 或抹掉 Kaggle 补源记录。
- **多因子展示名**: 统一策略详情、回测预检、回测归因和优化参数中的系统因子中文名，避免把 `s_mom_12m1m_rank` 等 canonical ID 当作业务展示名。
- **因子库表格**: 修复筛选下拉分散、最近诊断内容挤压图形，以及状态列把已完成诊断误显示为 Sandbox 可跑的问题。
- **多因子策略创建**: 权重、打分预览和策略门禁改为读取真实预览状态，已选因子权重支持调整并新增策略名称录入；有效覆盖率与得分分布跨度来自 PIT 表达式试算结果，缺少行业 PIT 时只阻断中性化，关闭中性化后可按真实门禁创建可回测策略。
- **OpenBB 固定收益增强层**: 固定收益补充源在本地 OpenBB 路由兼容性不足时会降级读取 provider 数据，避免可选增强层被记录为硬失败。

## [0.1.1-013] - 2026-05-04 - 新增多因子一期、因子默认基建等，并优化全路由首屏性能、首屏性能开发契约等

### 新增 (Added)

- **多因子一期**: 新增 PIT 清洗中心、因子库、因子详情诊断与因子编辑器的一期交付说明，默认覆盖估值、动量、质量、低波与规模五类常用因子。
- **因子默认基建**: 默认五类因子统一采用分层描述符 ID，并接入本地基础面 PIT 种子快照，使估值、质量和规模因子可进入诊断流程。
- **因子 Sandbox 准入**: 因子诊断新增 Sandbox 预览模式，完整 PIT 未补齐时仍可运行最近三年试算，但不会把因子升级为 Verified。
- **PIT 清洗中心可用性**: 新增覆盖率缺口下钻、MAD/3σ 清洗规则预览、Universe 历史成员变化图、复权因子轨迹和研究态 Limited Ready 豁免，帮助研究员先定位缺口再决定是否继续诊断。
- **PIT 数据治理闭环**: 覆盖率缺口下钻新增市值权重、时间分布、身份映射覆盖入口和数据运维指令，让研究员与运维能按影响优先级处理阻塞。

### 优化 (Changed)

- **全路由首屏性能**: 优化任务结果、资产库和 PIT/因子读取链路改为轻量首屏契约，减少历史大任务与组合详情扇出对工作台、优化结果页和数据治理页面的加载拖累。
- **首屏性能开发契约**: 工程文档新增轻量首屏、按需完整读取、短缓存和列表聚合规则，后续新页面开发与旧页面改造可按同一套性能门禁验收。
- **优化结果候选加载**: 优化任务的大候选集合不再内联进任务摘要首屏载荷，结果页先读取候选预览，用户打开全部组合时再按需补全候选集合。
- **资产库引用计数**: 资产库的策略腿引用计数改由统一腿部读模型直接返回，不再为每个组合追加详情请求。
- **PIT 与因子读取**: PIT 概览增加短时服务缓存并复用因子研究服务实例，降低因子库、因子详情和 PIT 清洗中心的重复读取成本。
- **因子库研判体验**: 相关性热力图改为按因子类别聚类并支持高相关筛选，最近诊断缩略图增加零线和正负双色区域，双因子比对与 Sandbox 缺口速报帮助研究员更快判断方向反转、竞品相关性和 Full Ready 阻塞原因。
- **因子库列表排序**: 因子列表新增最近诊断指标说明、Rank IC 排序、公式悬浮查看和最近更新时间排序，默认优先展示最新维护的因子。
- **债券快照覆盖**: LQD 债券 ETF 增加公开 tracking error 补充来源，并保留可审计来源信息；七行债券快照在字段齐备时可达到全部就绪与全部覆盖，顶部就绪度和资产腿入库门禁以当前七行契约为准。
- **PIT 门禁行动链路**: `PRICE_SNAPSHOT_NOT_READY` 等 PIT 阻塞项改为动态跳转到对应数据快照并高亮目标行；Limited Ready 只允许研究诊断，所有晋升语义继续要求 Full Ready。
- **PIT 阻塞原因表达**: PIT 顶部状态卡新增微缩阻断原因，研究态豁免会展示潜在 IC 扰动与市值权重估算，避免只看到笼统的阻塞状态。
- **因子库诊断反馈**: 因子库将相关性聚类改为可点击热力矩阵，并在最近诊断列直接展示缺失窗口或基础字段缺口；公式算子增加悬浮解释。
- **组合详情状态动作**: 组合详情页状态标签判定卡的处理按钮改为纵向主次操作组，避免窄栏下两个动作按钮拥挤换行。
- **导航信息架构**: 导航收敛为组合、策略、因子、数据四组；数据组保留 PIT 清洗中心与数据快照，数据快照页结构本期不调整。
- **工程文档真相**: 架构、设计与技术手册补齐因子占位路由、PIT 写入接口和 Sandbox 诊断状态，避免后续交付把占位入口误判为已接通工作流。

### 修复 (Fixed)

- **PIT 刷新并发稳定性**: 市场数据 companion SQLite 连接启用 WAL 与 30 秒 busy timeout，避免 PIT 清洗中心和快照刷新在页面轮询期间因数据库锁直接失败；研究态豁免写入后会立即刷新 PIT 概览缓存，公共供应商补齐后可通过 Limited Ready 继续诊断，正式晋升仍要求 Full Ready。
- **PIT 身份映射修复**: Identity Scraper 现在会批量使用可用身份来源，并在外部来源无法解析旧历史 ticker 时落入本地 PIT 锚点兜底，避免缺口清单长期停留在全量身份映射挂起。
- **组合回测历史版本**: 组合详情页的回测历史改为读取每次运行当时的组合配置版本，并用运行发起日展示历史卡片，避免旧记录被当前组合、完成时间或策略腿版本覆盖。
- **组合详情健康态精简**: 组合详情页在状态为“稳健：系统代理覆盖”或其它无执行类待办的稳健标签时不再展示“状态标签判定”模块，避免把已关闭的代理覆盖解释误呈现为待处理事项。
- **组合详情指标与诊断一致性**: 组合详情页指标区将净收益和最大回撤提前展示，70/30 基准图例展开权益/债券配置；组合列表和详情会用当前腿来源重算收益质量摘要，避免已由系统代理、现金规则或固定收益受管收益流覆盖的腿继续显示为问题腿。
- **因子详情操作区**: 因子诊断报告页的主操作按钮恢复为设计稿中的“保存为已验证”和投委会 PDF 双按钮样式，避免已完成诊断仍显示 Sandbox 提交语义。
- **因子热力图高亮**: 修复因子库热力图选中后高相关行在所有列重复出现绿色竖条的问题，现在仅因子列保留选中标记。
- **组合详情健康态精简**: 组合详情页在状态为“稳健：证据链完整”时不再展示状态标签判定卡，并将主操作改为“运行回测”进入组合回测配置页。
- **PIT 数据运维指令**: 修复“重启 Identity Scraper”只能打开说明、没有执行入口的问题，现在可直接执行身份映射重启任务并展示尝试、成功、失败和剩余待解析数量。
- **因子缺口速报**: 修复 Sandbox 缺口浮层正文不换行导致文字溢出的问题，缺口说明会在浮层内自然折行并保留后续操作可点击。
- **债券快照异常队列**: 债券治理页的异常队列只汇总固定收益来源自身的字段、曲线和来源登记问题，健康态不再混入股票或指数门禁；同类债券卡片完整进入滚动工作区，曲线提示改为系统诊断结论。
- **债券快照诊断折叠**: 债券快照页在无异常时自动收起质量审计、修复规则和原始快照区，并只展示需要处理的系统诊断，减少健康态页面长度。
- **债券快照修复链路**: 债券快照页保留完整刷新链路，可同时补齐股票价格、公司行为、指数估值、股票池历史和债券数据；审计矩阵、质量审计和异常行队列也改为中文可操作表达。
- **债券健康仪表盘**: 修复全量就绪时状态进度条仍显示待补和阻塞色段的问题，并在审计矩阵中把有值或正式豁免的推算字段归为可用，避免误判资产腿和组合可用性。
- **组合状态标签收益样本入口**: 组合列表与详情页中“收益样本窗口不足”会明确展示月度收益样本数量、折算年份和对齐缺口，并拆分为“调整来源样本”和“按短样本配置回测”两个动作，避免把回测配置误表述成补数据入口。
- **组合状态标签问题腿定位**: 组合详情页“状态标签判定”会列出收益样本不足和对齐缺口对应的组合腿，帮助操作者直接定位需要补历史或修复来源的对象。
- **组合腿样本预检**: 资产库与组合工作台来源库会提前展示收益样本不足和对齐缺口；现金规则腿不再被误判为缺失样本，详情页“查看来源证据”会直接定位到来源证据区。
- **组合固定收益来源质量**: 组合详情不再把 Treasury CMT/TIPS 曲线快照中的 par proxy 价格或过稀疏债券快照误算为真实收益历史；债券类资产腿与现金腿会使用受管收益 profile 覆盖，避免把策略、TIPS、T-Bill、债券 ETF 或现金腿误列为对齐缺口。
- **组合来源复核闭环**: 组合状态弹层新增确认并重新冻结来源指纹动作，操作者确认当前来源正确后可刷新冻结证据，并让弹层状态标签和动作按钮同步更新为已处理状态。
- **组合详情来源提示**: 组合详情页将策略腿同版本的冻结指纹复核与真实新版本提示分开显示，避免最新策略腿被误提示为版本漂移。
- **组合状态标签入口**: 组合回测列表、组合实验室和组合仪表板现在统一读取当前组合状态标签，并在待校准或失效时提供处理入口，避免旧作业残留误报失效或按钮点击无反馈。
- **组合详情状态一致性**: 组合详情页复用组合列表当前状态标签，并为逻辑一致性漂移提供处理弹层，避免同一组合在列表和详情页出现不同待办。

## [0.1.1-012] - 2026-04-30 - 新增组合中心 v2 全局索引页、组合版本与决策包闭环等，并优化组合状态标签治理、组合代理覆盖治理: 系统基建代理和人工确认代理不再进入待办等

### 新增 (Added)

- **组合中心 v2 全局索引页**: 新增组合列表、组合回测列表和组合实验室三类全局工作面，支持跨组合查看版本、状态标签、运行历史、候选方案和待决策事项。
- **组合版本与决策包闭环**: 新增组合运行历史持久化、候选方案晋升草稿版本、状态标签门禁和只读决策包，帮助投研保留来源、结论和版本记录。
- **资产配置策略配置页**: 新增策略级资产配置型创建入口，支持标的与权重、风险平价推荐、all-in / 定投、再平衡和成本模拟参数；策略详情、回测与优化继续复用现有策略页面，不创建第二套资产配置报告。

### 优化 (Changed)

- **组合状态标签治理**: 组合列表、详情、回测和 Allocation 页面统一以“状态标签”表达稳健、待校准和失效；弹层收敛为“前台判定说明 / 动作 / 解决判定”，并支持确认代理覆盖、刷新诊断和打开处理入口。
- **组合代理覆盖治理**: 系统基建代理和人工确认代理不再进入待办；未登记未确认的代理显示“待校准：代理覆盖待确认”，异常断档、无收益流和临时估算仍按失效处理。
- **组合中心 v2 验收口径**: 统一全局页面的模块节奏、状态标签、晋升审查和固定验证要求，避免候选绕过审查或决策包随运行态漂移。
- **组合回测与实验室闭环**: 回测结果支持压力场景复盘、订单场景过滤和状态标签展示；配置实验候选必须进入晋升审查后生成草稿版本。
- **组合回测压力复盘**: 回测详情页和全局回测列表复用三类极端行情压力窗口，列表列默认展示组合表现最差的窗口，右侧可下钻回撤、修复周期和相对抗跌表现。
- **策略工作台**: 策略看板改为展示最近编辑的 6 条策略，并统一工作台说明、看板说明和最近活动说明为更简洁的金融专业表达。

### 修复 (Fixed)

- **组合仪表板新版本提示**: 仪表板将冻结签名复核与真实策略腿版本更新分开判断，避免没有新版本时仍在组合卡和待办队列提示“有新版本”。
- **组合实验室审查台**: 组合实验室恢复组合、优化方法、状态和门禁筛选，作业表改为按任务、优化方法、预期绩效、晋升门禁、迁移成本和操作复核，并修复晋升审查卡片的中文展示与窄栏溢出。
- **策略库一键生成**: 策略列表缺少长期回测时，一键生成会同时提交当前版本的 10Y、20Y、30Y 回测记录，并在生成中置灰入口、刷新列表状态。
- **策略库版本收益**: 策略库现在只用当前参数版本的长期回测计算收益列与状态；新版本缺少回测时会展示“一键生成”并归入待回测。
- **组合列表版本与周期证据**: 组合列表改为展示 10Y 年化、夏普、回撤和 10Y / 20Y / 30Y 周期完整度；组合详情页同步显示当前配置版本，并移除列表中的“来源复核”和“正式版本”标签。
- **组合回测列表**: 全局组合回测页恢复组合、状态、压力窗口与状态标签筛选，表格改为按组合名、周期、状态标签、压力窗口和核心收益风险指标查看，并支持点击压力场景联动右侧窗口。
- **组合回测压力窗口**: 回测列表右侧压力窗口改为点击后展示详情，压缩重复卡片信息，并统一为更简洁的专业复盘文案。
- **组合列表运营台**: 组合列表改为真实筛选与视图保存，版本标签并入组合名，并将冻结签名复核与真实策略版本更新分开提示。
- **资产配置标的录入**: 标的搜索下拉默认收起，选择股票或债券快照标的后自动带出资产类别；标的列改为只读展示，并修复目标权重数字输入与一位小数展示。
- **资产配置优化配置**: 资产配置策略进入优化配置时，权重参数会自动按合计 100% 过滤有效组合数，避免把无效权重组合纳入优化预算。
- **优化结果另存**: 结果页重新过滤时保留原优化任务快照不变，并在预览后提供“保存新结果”入口，将当前过滤结果另存为新的优化任务。
- **资产配置策略配置**: 资产配置型创建页移除顶部参数状态卡，初始标的改为空篮子，标的与基准改从数据快照选择；风险平价推荐遇到缺失价格历史时不再静默输出等权结果。
- **组合回测策略内订单**: 组合回测订单流水会合并策略腿源回测的内部交易信号，建仓后可继续看到动量、网格等策略自己的买卖逻辑，不再只剩组合层再平衡记录。
- **组合回测详情操作**: 组合回测详情页标题统一为“回测详情”，右侧重跑回测会展示回测中和回测结束反馈，并且启动优化可直接进入组合优化配置页。
- **组合回测订单触发原因**: 组合回测订单不再把策略腿最近一次内部换仓原因拼接成组合再平衡触发原因，策略腿订单改为展示组合层建仓/再平衡动作和持仓穿透来源，避免误读为两个调仓逻辑同日触发。
- **组合优化草稿门禁**: 存在未关闭失效问题的候选会在执行决策卡提前显示门禁暂停并禁用草稿生成入口，避免操作者提交后才看到失败提示。
- **资产配置回测复盘**: 资产配置型回测的固定频率再平衡会在到期交易日生成订单，运行详情配置页会用中文展示参数快照与数据快照摘要，并隐藏例行权重归一提示。

## [0.1.1-011] - 2026-04-29 - 新增回测历史策略库视图，并优化组合资产配置微调、组合优化结果页等

### 新增 (Added)

- **回测历史策略库视图**: 回测历史新增按策略与参数版本聚合的证据树、智能任务栏和运行诊断，支持从缺失长周期证据直接补齐永久回测。

### 优化 (Changed)

- **组合资产配置微调**: 资产微调模块改为按当前权重降序展示，并支持滑块、数字输入、失焦校验与锁定/解锁，让候选生成使用当前约束。
- **组合优化结果页**: 候选方案可按最小波动、风险平价和最大夏普分组筛选，并将版本晋升入口集中到执行决策卡。
- **回测历史降噪**: 回测历史页的策略库视图保持只读证据树，移除勾选和对比模式；最近运行视图恢复历史回测列表的操作列与删除确认入口。
- **组合回测诊断页**: 组合回测结果页改为左侧曲线、右侧核心指标的平衡首屏，绩效矩阵切换为 `10Y / 20Y / 30Y` 对比卡，并将敞口审计说明、数据置信度和底层标的穿透集中展示，降低长说明文本对视觉节奏的干扰。
- **组合详情页模块收口**: 组合详情页将配置审计收敛为“配置版本记录”、将审计轨迹收敛为“操作日志”，移除深入分析入口，并对齐敞口穿透、风险归因和组合回测历史的卡片节奏。
- **组合仪表板加载性能**: 组合列表契约直接承载来源复核状态，页面不再为每个组合追加详情补水请求，提升首屏加载稳定性。

### 修复 (Fixed)

- **组合配置版本记录**: 组合保存与配置实验室晋升只有在结构参数真实变化并填写升级理由后才生成新版本记录；旧结构复核事件不再冒充版本升级，无有效版本历史时标题标签与版本记录都回退展示当前 `v1`。
- **回测详情执行窗口**: 回测详情配置页新增执行窗口卡片，明确请求开始、信号生效、首笔订单、测试集起点和执行策略，避免预热期与首笔成交日期被误读为订单缺失。
- **动量回测预热窗口**: 动量策略会从请求回测起点向前加载 lookback/skip 所需历史，只把该历史用于信号预热；收益曲线、有效起点与首笔订单重新对齐到用户请求的实际下单起点。
- **回测历史跳转**: 回测历史页的运行行点击会直接进入对应回测详情页，避免只能停留在列表侧栏诊断。
- **组合详情页 β 暴露指标**: 已保存组合详情会从组合收益和基准收益计算 β 暴露；旧分析缓存缺少该指标时会自动刷新，避免卡片误报“等待运行态β指标”。
- **组合优化风险平价候选**: Allocation job 的风险平价候选改为按各腿实现波动率反向分配并遵守锁定/上下界约束，不再把可调资产机械等权；有效前沿中仅基准组合保留红色空心点，未选候选点改为中性描边。
- **组合详情配置记录与指纹抽屉**: 组合详情配置版本记录改为按变更后版本倒序展示，并在卡片内汇总权重、再平衡频次和成本口径；配置指纹腿卡片改为打开一致的腿详情抽屉，同时压缩再平衡与成本模块高度。
- **组合优化结果可视状态**: 结果页进入时不再显示程序化焦点边框，候选选择器参考列不再展示无效动作文案，有效前沿的选中点、图例和权重快照保持同一候选语义。
- **组合优化结果基准对比**: Allocation job 结果页会把当前组合、基准组合与所选方案分开展示；候选选择器新增基准组合列并移除当前/基准的决策动作，压力测试改按真实收益窗口计算当前/基准回撤，候选只按运行时峰值回撤比例投影，不再使用前端硬编码倍数或同值 mock。
- **组合资产配置锁定权重**: 资产微调中上下界一致后锁定时，固定说明和现金约束标签会按所配置权重展示，不再回退到原始当前权重。
- **组合回测订单流水**: 全量流水首个事件改为组合建仓买入；若策略腿首笔成交晚于组合首期，会用该策略首个可用持仓生成初始建仓穿透，订单会输出模拟成交价、美元手续费，并把策略腿持仓穿透来源与组合建仓/再平衡原因分开展示。
- **组合回测流水筛选**: 全量流水筛选改为先按来源腿收窄，再展示该来源腿下的标的筛选；订单查询与导出同步支持来源腿过滤。
- **组合优化结果判读**: 组合优化结果页的有效前沿现在明确标注不可达到区域，目标下拉改为“选择目标”并移除候选计数；候选表和压力测试会按回撤、波动、修复周期与防御溢价方向使用警示色，避免把更差风险表现误判为正向改善。
- **组合优化结果页布局**: 候选选择器默认收起配置下拉并展示前三名候选，移除完整审计日志按钮与冗余版本晋升说明，降低结果页的视觉噪音。
- **组合优化结果指标**: 组合优化结果页改为读取运行时 allocation job 的当前组合候选指标，当前组合年化收益、夏普、波动与回撤不再使用静态前端基准。
- **组合回测订单穿透**: 组合回测订单会把策略腿调仓拆到实际底层标的，保留来源腿线索用于追溯，避免订单明细继续显示笼统策略占位标的。
- **组合回测订单页**: 组合回测订单页会按完整 10 年收益窗口生成调仓事件和订单流水，并在运行时数据为空时保持空态，不再回填示例订单。
- **回测证据树文案**: `策略 / 版本 / 运行` 首列改为策略摘要、版本说明与周期元信息，避免暴露参数版本 ID 或生成式长摘要。
- **组合资产配置交互**: 约束预检标签点击后会显示选中反馈并打开相关调整气泡；资产腿入口统一为“调整”，进入后可把锁定权重作为本轮可调约束提交。
- **组合资产配置结果页**: 组合优化结果页的候选、有效前沿、成本归因和按钮文案统一为金融专业中文表达，避免运行态页面混杂英文术语影响交付验收。
- **组合回测深链**: 从组合详情进入既有来源回测记录时，会按组合冻结证据生成诊断、订单与证据视图，不再出现找不到组合回测结果的错误。
- **组合详情页信息架构**: 组合详情页调整权益曲线下方的模块顺序，统一敞口穿透、风险归因、配置迭代和配置指纹术语，并消除来源漂移英文提示。
- **组合资产配置生成**: 组合优化实验室的生成按钮会创建候选方案任务并进入结果页，避免点击后停留在配置页无反馈。
- **回测历史入口**: 回测历史页恢复策略证据树、最近运行诊断和页面标题，避免应用级构建或路由验收被缺失页面阻断。
- **回测证据状态**: 带提醒的已完成回测会归入“存在漂移”复核状态，不再被误判成证据断裂。

## [0.1.1-010] - 2026-04-28 - 新增组合中心 Split 工作流、组合层投研契约等，并优化组合详情资产管理中心、策略库入口等

### 新增 (Added)

- **组合中心 Split 工作流**: 新增组合级回测配置、诊断 / 订单 / 证据结果页和组合优化实验室 Split 工作流，支持稳定性诊断、订单流水过滤、内部对冲下钻、CSV / XLSX 导出、有效前沿候选和迁移成本评估。
- **组合层投研契约**: 新增组合回测运行、订单流水、内部对冲说明和资产配置任务契约，缺失长期历史、代理覆盖和真实成交边界会在页面显式标注。
- **策略参数版本记录**: 策略版本会保存系统生成的变更摘要、用户决策备注、来源任务 / 回测 / 候选线索、替代版本和可回滚状态。
- **历史参数回滚**: 策略详情支持“回滚至该版本”，回滚会复制历史参数生成新的当前版本，保留原始历史记录。

### 优化 (Changed)

- **组合详情资产管理中心**: 组合详情页从信号展示转为归因与风险协同视角，强化收益流可读性、七项 KPI、版本演化、执行历史和回测 / 订单深链入口。
- **策略库入口**: 策略库入口改为正式策略列表，长期年化收益列同步展示夏普比率；缺失长期回测可一键进入对应周期的回测创建页，列表默认按最近编辑时间倒序并支持年化收益排序。
- **策略参数历史**: 策略详情参数历史表收敛为“版本 / 变更摘要 / 决策说明 / 操作”，隐藏内部参数版本 ID，并把相邻版本参数差异按条换行展示、回写既有版本记录。
- **优化结果晋升**: “晋升当前版本”改为先确认候选摘要、参数差异、来源任务 / 回测和替代候选，用户只需填写决策备注。

### 修复 (Fixed)

- **优化结果晋升描述**: 优化候选晋升后会同步刷新策略描述、参数历史和确认字段，避免回测详情显示的说明参数与真实执行参数不一致。
- **策略参数历史**: 决策说明为空、旧占位或问号乱码时统一显示为 `-`，避免线上策略详情继续暴露无效说明文案。
- **策略参数历史表格**: 参数历史的变更摘要列改为优先保留横向阅读空间，并修复版本列分隔线提前断开的问题，避免多条摘要在窄面板中撑破行高。

## [0.1.1-009] - 2026-04-27 - 新增OpenBB 快照增强层、组合审计事件表，并调整组合详情指标收口、快照数据源门禁等

### 新增 (Added)

- **OpenBB 快照增强层**: 新增可选 `openbb-provider` extra 与 lazy adapter，在 `GRIT_ENABLE_OPENBB_PROVIDER=1` 时可接入 OpenBB 价格、固定收益曲线和当前成分辅助校验；默认安装与启动仍保持 OpenBB-free，缺包或缺 key 不会阻断本地服务。
- **组合审计事件表**: 新增 `composition_audit_events` append-only 后端表，持久化组合创建、结构 PATCH、状态切换、来源冻结、再平衡检查和债券快照刷新影响检查，详情 API 不再只依赖临时拼装审计轨迹。

### 优化 (Changed)

- **组合详情指标收口**: `#/compositions/:id` 指标区改为总收益、年化、预估净年化、波动、最大回撤、夏普比率、索提诺比率顺序；净年化按毛年化扣除滑点、现金缓冲、维护成本和调仓损耗估算，卡片内不再展示差值、样本、窗口或对齐冗余字段。
- **快照数据源门禁**: OpenBB provider 会通过既有 `provider_summary`、债券 `provider_results` 和 raw evidence 暴露，不改变 `/data-snapshots/overview` contract；OpenBB 当前成分只写辅助校验 metadata，不能替代 PIT 历史锚点或让 universe snapshot 变为 `READY`。
- **Phase 1.2 后端可信计算收口**: 组合预演与详情的收益点新增毛收益、净收益、维护成本、滑点、调仓成本和现金缓冲拖累拆解；调仓事件改用对齐收益流推导权重漂移，风险贡献优先使用协方差贡献，债券腿会把久期与凸性带入风险预算字段。
- **资产腿来源选择体验**: `#/legs` 创建资产腿抽屉的来源选择现在最多露出 3 张资产卡片，更多候选保留在滚动容器内；运行时债券资产卡片以“中文名 英文名”展示，选中资产后腿名称默认填充中文名。
- **Phase 1.2 组合可信度增强**: `POST /compositions/preview` 与 `GET /compositions/{id}` 现在补充收益流质量、真实调仓事件、来源签名/漂移、审计轨迹和扩展风险贡献字段；`#/compositions/workbench`、`#/compositions/:id`、`#/legs` 与 `#/snapshots?tab=bond` 已按批准稿单方向增强，展示净收益拆解、冻结哈希、版本漂移、债券质量审计、日频应计利息和久期/凸性风险预算预检。
- **策略创建支持动态定投逻辑提取**: `#/creation/sessions/:id` 现在会从定投 prompt 中提取“定投执行锚点”和“动态定投逻辑”，支持识别类似 `QQQ 动态定投策略 + 每月第一个交易日 + 滚动10年 PE(TTM) 估值百分位倍率表` 的输入，并在创建页与策略详情中展示动态倍率规则，同时保持普通定投表单不出现多余空字段。

### 修复 (Fixed)

- **优化结果晋升基线**: `#/optimization-jobs/:id` 点击“晋升当前版本”时会随请求提交当前已加载策略的 `current_parameter_version_id`，让旧优化任务能按最新策略版本完成晋升，同时保留后端对显式旧基线的 `409 stale_base_parameter_version` 保护。
- **优化结果候选盘收口**: `#/optimization-jobs/:id` 的“参数候选盘”和“快速切换候选版本”现在只展示排序后的前 3 个符合约束组合并保留“当前组合”基准行；“查看全部组合”弹窗继续展示全量匹配组合，且当任务源回测已失效时会回退读取策略最新完成回测作为基准。
- **组合仪表板卡片指标**: `#/compositions` 组合卡片现在用运行时列表契约返回的夏普比率替换前端估算波动率，避免卡片继续展示推导占位指标。
- **组合详情页指标与归因呈现**: `#/compositions/:id` 总收益卡片收紧为“总收益 + 右侧超额”一行，并只在对比区保留基准值；年化、预估净年化、波动、最大回撤、夏普和索提诺展示与基准差值；来源签名与风险归因按权重降序展示，风险卡片改为权重/收益占比/风险占比摘要，相关性矩阵补齐腿名称轴并移除多腿关系选项。
- **组合工作台来源指标标签**: `#/compositions/workbench` 来源库卡片底部标签改为按腿类型展示策略类型、年化收益、最大回撤、夏普、债券 YTM/久期/波动或现金来源规则，不再把 `universe:*` 等内部属性标签直接暴露给操作员。
- **组合相关性与冻结审计语义**: 无真实对齐收益流时，相关性矩阵不再回退到 hash 合成相关性；来源漂移只写提示和审计事件，不会改写 `composition_source_freezes` 的冻结哈希。
- **股票快照权益口径**: `#/snapshots` 股票/指数 tab 现在把标普与纳指成分股名单计入“权益篮子”可用性，最新刷新卡片改为展示本次新增数据行与标的数，`指数与基准` 上下两处统一按 SPY/QQQ 基准 ETF 历史价格覆盖判断完备度。
- **组合详情页收益窗口与现金权重生效**: `GET /compositions/{id}` 与 `POST /compositions/preview` 现在默认按最近 120 个自然月生成组合收益、基准、超额收益与指标；当现金腿或资产腿缺少可对齐收益流时，会用腿配置画像补齐 fallback 收益并在 `return_quality_summary` 标记质量缺口，避免编辑为月度再平衡或把现金占比从 30% 调到 5% 后指标/收益图仍只跟随单一策略腿。
- **组合详情页累计收益图悬浮层**: `#/compositions/:id` 的累计收益图新增鼠标悬浮数据弹层，按回测详情页同类样式展示当前点的组合累计收益、基准累计收益与超额收益。
- **组合详情页收益流与来源签名收口**: `#/compositions/:id` 累计收益流移除再平衡 tab、右上基准标签和独立超额收益小图，将回撤阴影与再平衡点直接叠加到主图；来源签名卡片改用腿中文名作为标题，并把右侧状态胶囊改为腿类型标签。
- **组合工作台 Phase 1.2 UI 收口**: `#/compositions/workbench` 的收益流质量、真实调仓事件、来源签名与净收益拆解已从独立大卡收回到现有“成立性评分 / 再平衡频次 / 摘要字段”模块内，保留当前线上三栏结构并补齐轻量批准稿、Trace Matrix、focused test 与浏览器截图证据。
- **Phase 1.1 静态逻辑回正**: `#/compositions` 的“组合观察”改为从真实组合列表派生最高年化、最大回撤与平均评分，不再渲染固定 SVG 曲线、`最近 90 日` 或 `ETF 腿相关性略升` 等静态文案；`#/legs` 创建策略腿统一复用运行时候选构建器并读取最新回测 OOS 指标，创建资产腿在权益运行时来源契约落地前不再暴露 `Equity-*` 静态快照源。
- **组合详情页 Compose First 布局回正**: `#/compositions/composition_e40eb6b29475` 现在按 `compose-first-2026-04-21` 批准稿恢复七张 KPI、`1.78fr / 330px` 主体、累计收益流主卡、右侧“来源快照 / 再平衡与成本 / 深入分析入口”rail 与中文运行态文案，避免旧版/其他方向稿继续覆盖 live 详情页。
- **组合详情页首屏与审计体验**: `#/compositions/composition_e40eb6b29475` 的来源签名 rail 已收紧为首屏内展示并防止长来源 ID 横向撑宽；总收益说明、超额收益小图、审计轨迹中文化和深入分析入口同步收敛为更清晰的运行态表达。
- **资产腿债券来源保存回正**: `#/legs` 创建资产腿抽屉现在从 `GET /data-snapshots/overview` 的 `bond_fixed_income.eligible_instruments` 构造债券来源，默认不再用 `Bond-UST10Y-EOD-*` 静态来源过滤列表；T-Bill 可被选择并按真实 `bond_fixed_income::UST_BILL_3M::*` 快照保存入库。
- **策略腿引用数与详情抽屉回正**: `#/legs` 保存的策略腿现在会从未归档组合详情反查引用数，`#/compositions/{id}` 读取临时策略腿时也会返回真实引用摘要；腿部详情抽屉补回内部滚动容器与长 ID 断行，避免来源签名区和操作区在窄屏下叠压。
- **逻辑删除二次确认**: `#/compositions` 组合归档与 `#/legs` 资产库归档现在都会先打开确认浮层，明确对象名称、稳定 ID 和逻辑删除含义，取消时不会触发 `ARCHIVED` 写入；项目规范同步要求所有用户触发的逻辑删除都必须二次确认。
- **指数估值快照与 QQQ 动态定投真实回测**: market-data companion 新增 `ds-index-valuations`、`dataset_index_valuations` 与 `dataset_index_valuation_coverage` 数据面；`#/snapshots` 股票/指数 tab 现已显示估值快照行但不把它纳入全局阻塞；`BUY_AND_HOLD` 动态定投会在真实回测路径里按 `QQQ -> nasdaq100` 代理读取月频估值百分位，逐笔写入 `contribution_multiplier`、`valuation_percentile_10y` 与 `valuation_bucket`，不再把 `dynamic_investment_logic` 一律降级成“未执行” warning。
- **组合/腿部归档语义回正**: `#/compositions` 不再展示 `ARCHIVED` 组合，`#/legs` 的腿部引用组合数改为仅统计未归档组合，且未被引用的资产腿/现金腿现在可以通过运行时 PATCH 正常归档；归档后的组合与腿继续保留在库中作为逻辑删除记录，但不会再出现在其他业务入口或被后续组合流程继续引用。
- **组合详情页收益/归因/情景分析回正**: `#/compositions/composition_e40eb6b29475` 这类已落库的组合详情现在会在读取时自动识别旧版 `analysis_json` 并重建运行态分析，补齐 `volatility`、`cash_weight`、`Sharpe`、`Sortino`、`return_contribution_pct` 与 3 张情景分析卡；前端详情页同时切回批准稿的累计收益流、风险归因热区、相关性热力矩阵与情景卡片结构，避免旧占位口径和简化版面板继续出现在 live 页面。
- **动态定投回测告警与成交明细回正**: `BUY_AND_HOLD` 回测现在会为每笔定投持久化真实 `quantity/net_amount`，`#/runs/:id` 不再把后续定投订单渲染成 `0`；当策略声明 `dynamic_investment_logic` 但估值快照尚未就绪时，预览与提交会在策略级快照检查阶段直接阻塞；当单笔交易只能拿到超过 45 天的旧 observation 时，回测会只对该笔回退到 `1.0x` 并写入显式 warning，不再把整个动态定投策略页固定降级成“未执行”提示。
- **债券快照头部与工作站稳定性**: `#/snapshots?tab=bond` 与股票 tab 的顶部模块统一命名为“健康仪表盘”，并收紧头部内边距与卡片密度，让页签切换时标题、副标题保持同一屏幕位置；债券三位一体工作站改为仅展示 3 张按久期升序排列的来源卡，并新增剩余进度条、移除曲线下方期限圆点文案；“原始快照与调度”模块同步收敛为中文短文案，清除 `Runtime fixed-income snapshot row` 等英文实现词。
- **组合工作台来源卡内容架构回正**: `#/compositions/workbench?composition_id=...` 的来源库卡片已按批准稿恢复为“标题 → 特征 → 来源简介 → 标签”顺序；现金腿卡片不再擅自拼接 `TARGET_BUFFER`、`phase1_live_smoke_cash` 等内部版本/烟测文案，改为输出规则名、维护状态、成本吸收说明与组合约束标签。
- **数据快照债券/股票页进一步收口**: `#/snapshots` 股票/指数 tab 已移除三位一体工作站标题右侧“股票 / 指数 / 篮子”标签，并让“股票底库”进度条分子分母与头部股票快照覆盖率卡片保持同源；债券/固定收益 tab 已移除全局健康仪表盘中的 `UST/TIPS/IG x/x 就绪` 标签与“自愈进行中”芯片，将“入库链路与数据自愈”更名为“入库链路”，同时把三位一体工作站恢复为按 UST/TIPS/IG 分组驱动的左支柱、中选中债券、右曲线预览结构，确保资产腿 rail 继续跟随当前选中 runtime 债券来源。
- **组合工作台来源卡与收益预览对齐**: `#/compositions/workbench?composition_id=...` 已移除来源库与结构总览中的紫色“已被引用”“已锁定”胶囊标签，来源库腿卡片收紧为批准稿密度，并把收益流预览改为组合收益与基准共用同一纵轴；本地 live 验收同时要求重载 `4173` 预览 bundle 并确认 `8000` 后端已切到当前源码，避免旧进程继续返回 `P1..P6` 合成收益序列。
- **组合工作台实时语义**: `#/compositions/workbench?composition_id=...` 现在复用 `#/legs` 已保存策略腿名单而不是自行投影全部最新回测候选；来源库“当前选中”按钮改为“已加入”并改用灰底卡片态，结构总览移除重复比例摘要与“相关性提醒”计数标签，摘要诊断区移除“来源可信度”行并简化维护判断文案，收益流预览改为优先读取当前组合腿的真实收益/回撤时间序列，仅在缺少历史时回退兜底数据。
- **腿定义编辑与复制保护**: `#/legs` 创建策略腿默认命名为“策略名-vN”；资产腿/现金腿创建与编辑抽屉支持语义字段保存；被组合引用的资产腿/现金腿改为复制入口，后端对引用保护 PATCH 返回 `409 leg_reference_protected`，并对同源同核心参数复制返回 `409 duplicate_leg_definition`。
- **债券快照七行契约回归**: 补齐七条债券/固定收益运行时 fixture 与回归覆盖，守住 `asset_type`、`tenor_label`、`audit_profile`、`discount_rate_pct`、`real_yield_pct`、`inflation_factor`、`breakeven_inflation_bps`、`effective_duration`、`sec_yield_30d_pct`、`credit_quality`、`tracking_error_bps`、`audit_alerts`、`audit_notes` 与 `tracking_status` 字段；LQD `WATCH` 行会被拒绝创建资产腿，T-Bill 应计利息缺口按贴现券豁免，TIPS breakeven 与 UST 利差异常保留审计证据。
- **组合工作台对齐**: `#/compositions/workbench?composition_id=...` 现在会用已加入腿数对真实可用来源数展示来源进度，并把组合名称、描述与基准说明移出右侧摘要诊断 rail，恢复 Compose First 工作台的中列配置、右侧诊断和保存区顺序。
- **腿部清单标签筛选**: `#/legs` 状态列现在每条腿只显示“最新版本/待更新”和“已被引用/未引用/孤儿腿”两类标签，右侧标签筛选支持再次点击已选中项取消筛选。
- **资产库新建腿抽屉对齐**: `#/legs` 的新建策略腿、资产腿与现金腿抽屉已按 Compose First 批准稿恢复右侧 inset shell、头部动作区、内部滚动、来源候选栈、身份/核心参数/验证摘要字段与现金规则内容，并补齐专项 UI Trace Matrix、内容结构回归测试和浏览器 DOM/截图验收证据。
- **资产腿预设抽屉收口**: `#/legs` 创建资产腿抽屉已按 `asset-leg-type-preset-2026-04-24` HTML/SPEC 收敛为“资产类型 / 来源选择 / 身份与角色 / 核心规则 / 验证摘要”五段结构，支持债券与股票中文预设切换、快照检索、来源选择联动和 KPI 中文化，并补齐专项 Matrix、Vitest 与浏览器 DOM/截图验收证据。
- **腿部清单编辑与展示**: `#/legs` 资产腿和现金腿编辑抽屉现在会写回本地定义并刷新清单；策略腿编辑抽屉现在支持名称、冻结口径与备注本地保存；策略腿可见 ID 收敛为 `策略id::快照id`，类型列移除次级说明，引用列改为仅显示引用组合数。
- **Compose First UI 一致性**: 已根据 `compose-first-2026-04-21` HTML/SPEC 输出五页 UI Trace Matrix，并把组合仪表板、资产库、组合详情、股票快照与债券快照中的状态写入按钮、保存态、PIT/指标/来源文案和 runtime 提示统一到批准稿中文表达；详情页 hero 动作区也收敛为稳定的响应式布局。
- **股票快照专项验收**: 新增 `#/snapshots` 股票/指数 tab 专项 UI Trace Matrix，并把全局视角、工作站、诊断 rail 与原始清单中的 `runtime`、`overview`、`mixed_sources`、`Ready`、`Pending` 等实现词收敛为批准稿中文业务表达。
- **数据快照页签切换一致性**: `#/snapshots` 股票/指数与债券/固定收益 tab 现在共用同一套头部样式与专业说明文案，移除债券页头部额外标签，并统一两个 tab 的全局仪表盘高度和外层节奏，避免切换时产生首屏跳动。
- **股票快照视觉密度**: `#/snapshots` 股票/指数 tab 移除页头、全局视角、指标卡、三位一体工作站、诊断 rail、原始清单和就绪标准中的过大固定高度，并把工作站与原始清单改为同一左列栈，避免右侧诊断 rail 撑高后留下大块空白。
- **数据快照页进一步对齐批准稿**: `#/snapshots` 股票/指数与债券/固定收益 tab 进一步压缩全局视角空白并统一三位一体工作站头部高度；股票 tab 改为沿用债券页字体栈，股票快照覆盖率按公司行为数据与股票价格数据合并计数；债券工作站恢复为批准稿要求的三张债券卡加内部滚动结构，切换页签时不再因头部节奏和卡片高度差异产生明显抖动。
- **组合详情页专项验收**: `#/compositions/composition_91f49df44f63` 现在按批准稿固定展示总收益、年化、波动、最大回撤、现金占比、Sharpe 与 Sortino 七项 KPI，Hero 动作恢复为复制、重新平衡、编辑组合，来源快照 rail 不再暴露运行态内部 ID 或状态写入动作。

## [0.1.1-008] - 2026-04-23 - 新增Phase 1.1 组合落库闭环、债券快照真实接入，并调整资产库腿部清单投研化、Compose First 五页真实 runtime等

### 新增 (Added)

- **Phase 1.1 组合落库闭环**: 组合一期表补齐 revision、当前冻结代、状态、软删除字段、唯一约束与索引；`preview` 保持零写入，`create/patch` 会原子写入组合腿与 `composition_source_freezes`，详情页读取冻结来源而不是实时漂移数据。
- **债券快照真实接入**: market-data repository 新增 `bond_fixed_income_snapshots` 读写面，`GET /data-snapshots/overview` 的 `bond_fixed_income` 现在公开 eligible runtime sources / instruments，并支持从合格债券快照创建资产腿。

### 优化 (Changed)

- **资产库腿部清单投研化**: `#/legs` 清单表头从行政字段改为投研决策字段，行内直接展示 PIT 快照、YTM/久期/波动率、策略年化/回撤、现金缓冲、引用渗透率与冻结/闲置状态；操作列统一为“详情 / 编辑 / 查看来源 / 归档”，并支持点击行打开来源审计详情抽屉。
- **Compose First 五页真实 runtime**: `#/compositions`、`#/legs`、`#/compositions/workbench`、`#/compositions/:id` 与 `#/snapshots?tab=bond` 均通过 `ApiClientProvider` 调用真实 HTTP API；dashboard/detail 支持组合状态写入，workbench 支持 create/patch，资产库与债券 tab 支持真实资产/现金腿写入入口。
- **本地 live 交付门禁**: `http://127.0.0.1:4173` 页面验收必须核对最终代码对应的 backend/preview 监听进程、重建 preview bundle，并用真实浏览器与 API 结果确认，避免旧进程或旧 bundle 被误判为已交付。
- **生产路由隔离静态预览**: 旧 `#/compositions/detail` 设计预览别名不再进入生产详情页路由，避免把静态批准稿误当作真实组合详情。

### 修复 (Fixed)

- **策略腿刷新保留**: `#/legs` 保存策略腿候选后会记录稳定的 `strategy_leg::{strategy_id}::{parameter_version_id}` 引用，页面刷新后继续用最新候选数据恢复到资产库清单，不再让“标普动量策略”等已保存策略腿从列表中消失。
- **数据快照视觉回归**: `#/snapshots` 股票 tab 的全局视角指标、EST 刷新时间、原始清单字段/调度列、hero 高度、全局面板高度、工作站卡片高度与就绪标准 rail 重新对齐 Compose First 批准稿；债券 tab 恢复全局健康仪表盘、三位一体工作站、资产腿创建 rail、影子数据审计矩阵/系统诊断双栏、原始调度区按钮位置与批准稿视觉密度，同时继续绑定真实 runtime 快照行。
- **QuickStart 预览端口重启**: `QuickStart-Grit.ps1` 现在能识别由 `node ./preview-server.mjs --watch --rebuild-on-start` 启动的 repo-local 预览进程，并在重启 `4173` 时正确停止旧监听，不再误报 `non-repo frontend process`。
- **Workspace 与快照 live 数据**: 本地 `#/workspace` 验收恢复使用有历史策略/回测的 runtime SQLite 数据，`#/snapshots` 股票/指数 tab 继续映射 `GET /data-snapshots/overview` 的 `dataset_snapshots[]` 与 `universe_snapshots[]`，但保持 Compose First 批准稿的全局视角、三位一体工作站 + 数据诊断报告、原始快照清单 + 就绪标准两组双栏结构，不再回退到 `99.8%` 或 `Universe-US-Equity-*` 等静态设计稿值。
- **数据快照债券稿对齐**: `#/snapshots?tab=bond` 保持批准稿的就绪/待补/阻塞、影子字段覆盖率、入库链路与数据自愈、三栏工作站、资产腿创建侧栏、影子数据审计矩阵、批量修复规则和原始快照与调度结构；页面只展示真实 `bond_fixed_income` runtime 行，资产腿创建侧栏会跟随当前选中的 runtime 债券来源，不再展示 `Bond-UST*`、FMP/Polygon 延迟、phase1 curve proxy 等 mock 文案。
- **资产库策略腿候选恢复**: `GET /leg-inventory` 与 demo store 仍不把所有策略参数版本自动投影进默认资产库，`#/legs` 默认只展示人工创建/持久化的资产腿与现金腿；“创建策略腿”抽屉会按每个策略的最新已完成回测运行构造临时 `strategy_leg::{strategy_id}::{parameter_version_id}` 候选，并用所选候选的指标驱动右侧验证摘要曲线和 KPI，避免策略库已有合格版本时误报空态或只显示前三个候选。
- **策略腿入库反馈**: `#/legs` 保存策略腿候选后停留在策略资产库，显示“创建策略腿成功”提示，并把选中的策略腿投影加入当前腿部清单，不再直接跳转组合工作台。
- **数据快照债券视觉密度**: `#/snapshots?tab=bond` 去除债券模块前的固定大空白，并统一页签、状态标签和面板阴影密度，避免债券 tab 与股票 tab 视觉脱节。

## [0.1.1-007] - 2026-04-23 - 调整UI 交付门禁，并修复组合详情页、策略资产库等

### 优化 (Changed)

- **UI 交付门禁**: 已把批准 HTML/SPEC 的实施流程补齐为 manifest、Trace Matrix、截图、DOM 文案扫描、CSS 尺寸契约、交互证明与 coder 交付前自测并行验收，避免只凭 focused tests 或类名存在断言放行设计漂移。

### 修复 (Fixed)

- **组合详情页**: `#/compositions/detail` 现在作为 Compose First 批准稿公开预览路由渲染，不再把 `detail` 当作真实组合 ID 请求后端；首屏 KPI、累计收益流、来源 rail、相关性切换与来源快照抽屉已按批准 HTML/SPEC 对齐。
- **策略资产库**: `#/legs` 已按 Compose First 批准稿还原 hero、全局统计、筛选选中态、过滤工具栏、清单密度、管理原则任务卡、`+ 新建腿` split menu 与策略腿宽抽屉交互，避免资产库主态和创建入口偏离设计稿。
- **组合仪表板**: `#/compositions` 已按 Compose First 批准稿收敛首屏结构、组合卡数量、待办卡数量、最近活动列表与组合观察图，避免正式组合卡、待办和活动区过度铺陈导致首屏偏离设计稿。
- **组合工作台**: `#/compositions/workbench` 裸路由会按 Compose First 批准稿自动带出 starter 组合草稿，并补齐紧凑间距、全局字号、来源库四张完整策略卡高度滚动、摘要评分拆解、成立性评分圆环/五边形评分、相关性风险色阶、结构比例图标注、收益流回撤/时间段预览和保存区模块底部吸底，避免首屏停在空组合状态或诊断区降级为普通表单。
- **股票指数数据快照**: `#/snapshots?tab=equity` 已按 Compose First 批准稿还原全局视角、三位一体工作站、原始快照清单、数据诊断报告与就绪标准；三位一体工作站已移除强制卡片高度并收紧视觉密度，同时补齐 UI Artifact Trace Matrix、像素度量截图和待补筛选交互。
- **债券数据快照**: `#/snapshots?tab=bond` 已按 Compose First 批准稿还原债券/固定收益页签的标题区、全局健康仪表盘、三位一体工作站、影子数据审计矩阵与调度治理区，不再回退到共享快照路由的旧内容模型。

## [0.1.1-006] - 2026-04-22 - 新增组合一期对象面、Compose First 页面链路，并调整数据快照总览、固定验证入口

### 新增 (Added)

- **组合一期对象面**: 新增 `asset_leg_definitions`、`cash_leg_definitions`、`compositions`、`composition_legs` 与 `composition_source_freezes` 最小持久化结构，并开放 `GET /leg-inventory`、`POST /asset-legs`、`POST /cash-legs`、`GET /compositions`、`GET /compositions/{id}`、`POST /compositions/preview`、`POST /compositions`、`PATCH /compositions/{id}` 一期契约面。
- **Compose First 页面链路**: 新增 `#/compositions` 组合仪表板、`#/legs` 资产库、`#/compositions/workbench` 组合工作台与 `#/compositions/:id` 组合详情页，并把新页面接入现有 shell/runtime 与中文导航分组。

### 优化 (Changed)

- **数据快照总览**: `GET /data-snapshots/overview` 追加 `bond_fixed_income` 分段，在原有 `#/snapshots` 页面内扩展债券/固定收益页签，不再另起第二套快照路由或 API。
- **固定验证入口**: `scripts/codex-test-backend.ps1` 与 `scripts/codex-test-frontend.ps1` 现已纳入组合 API、组合四页、债券快照页签与新的 shell/route 集成测试，避免一期能力只靠手工补跑维持回归门禁。

## [0.1.1-005] - 2026-04-21 - 修复快照就绪标签、回测默认执行成本等

### 修复 (Fixed)

- **快照就绪标签**: `#/snapshots` 中 `READY/就绪` 卡片现在会按真实状态显示绿色成功标签，不再因为附带非阻塞 `blocker` 元数据而被误渲染成红色。
- **回测默认执行成本**: 真实回测请求现在默认补齐 `fee_bps=1.5` 与 `slippage_bps=2.5`，并将两者合并写入回测引擎交易成本；新增 `python -m grit_backtest_platform.main backfill-backtest-costs` 用于按同一默认值回刷永久保存的历史回测结果。
- **免费源补齐链**: 快照 repair 现在按 `public -> free_account -> paid_optional` 调度市场数据源；`Yahoo/yfinance` 成为默认价格与公司行为探针主链，`Alpha Vantage DIVIDENDS/SPLITS` 作为免费账号修复通道补齐公司行为，`Tiingo` 仅在 token 可用且未处于冷却窗口时参与额度感知修复。
- **公司行为覆盖口径**: `ds-corporate-actions` 现在会把 `probe_complete=true` 且确认“无正式 dividend/split/reverse_split 事件”的 symbol 计入覆盖，并稳定暴露 `complete_no_events_symbol_count`、`formal_event_symbol_count` 以及 provider quota/cooldown 元数据给 `#/snapshots`。
- **Nasdaq-100 历史锚点**: 股票池历史链新增本地官方 provenance 种子与 Internet Archive 官方 Nasdaq activity 页回填；`un-ndx100` 额外公开 `official_seed_status`、`official_seed_source_count` 与 `official_seed_missing_anchors`，便于直接追踪离 100% 还差哪些锚点。
- **优化结果重新过滤**: `#/optimization-jobs/:id` 在已保存候选全部不满足当前阈值、但全量 `matching_combinations` 仍有命中组合时，不再被前端误判为“无符合条件的组合”；结果页会继续使用全量命中组合完成重新过滤与候选展示，不再出现“筛出 3 个组合却只展示 1 个”的结果中心偏差，`最大回撤 <= 2%` 这类紧约束现在可正确筛出实际满足条件的试验。
- **候选版本中文展示**: `#/optimization-jobs/:id` 在读取全量 `matching_combinations` 且候选名仍为 `Trial xxxx` 时，结果中心与底部“快速切换候选版本”会统一回退展示为 `候选 N`；卡片摘要中的 `daily / weekly / monthly / sharpe / oos / max drawdown` 也会同步翻译为中文，避免候选版本名和摘要说明混入英文。
- **候选晋升 trial id**: `#/optimization-jobs/:id` 从全量 `matching_combinations` 选中的 `trial_*` 组合现在也可直接“晋升当前版本”；后端会按匹配组合或持久化 trial 记录解析待晋升参数，不再因只扫描 `candidates_json` 而报 `Optimization candidate not found`。
- **优化参数摘要**: `#/optimization-jobs/:id` 的顶部“参数组合”、参数候选盘和“查看全部组合”弹层现在会同时展示离散搜索维度与范围搜索维度，均值回归策略的 `观察周期` 不再遗漏 `每日 / 每周 / 每月` 选项。
- **约束条件顺序**: `#/optimization-jobs/:id` 与 `#/optimization-jobs/new/config` 的“约束条件”模块已将 `收益夏普` 与 `样本外夏普` 按预期互换位置，旧任务读取历史约束时也会按当前预设顺序稳定展示。
- **约束阈值输入**: Optimization Lab 配置页与结果页的约束阈值输入框清空后不再被立即回写成 `0`，现在会保留空白编辑态，并在失焦时恢复到当前已生效阈值。

## [0.1.1-004] - 2026-04-17 - 调整目标排序与约束条件展示，并修复 QuickStart 预览与结果弹层体验

### 优化 (Changed)

- **目标排序**: Optimization Lab 配置页与结果页统一支持 `收益夏普 Max / 年化收益率 Max / 综合得分 Max` 三种目标排序，重新过滤与结果排名现在都会按当前目标字段严格主键重排。
- **约束条件**: Optimization Lab 结果页将“快捷过滤”统一改名为“约束条件”，并移除顶部预设标签、符合约束计数与逐条约束标签串；配置页与结果页对外公开约束同步收口为 5 项，不再暴露 `换手率`。
- **策略详情摘要**: `#/strategies/:id` 标题下方不再展示类型 / 股票池 / 基准 / 再平衡等标签串，改为基于当前参数生成一句策略摘要，直接概括信号逻辑、持仓规则与调仓节奏。

### 修复 (Fixed)

- **QuickStart 前端预览**: `QuickStart-Grit.ps1` 现在会以 `--rebuild-on-start` 启动 `web/preview-server.mjs`，避免默认 `http://127.0.0.1:4173/#/workspace` 在冷启动时返回 `Frontend build not ready yet.`。
- **查看全部组合弹层**: `#/optimization-jobs/:id` 的“查看全部组合”弹层改为桌面安全宽度，避免结果表在电脑屏幕上横向撑出滚动条；底部操作栏固定；弹层打开时会锁定背景页面滚动，并将头部/底部区域的滚轮转发到表格区，保证弹层与表格都能顺畅使用滚轮浏览。
- **演示 API 合同**: `web/src/lib/demoStore.ts` 与 `web/src/lib/demoStorePhase4.ts` 补齐 `updateOptimizationJobConstraints`，前端全局 `tsc --noEmit` 不再被这两条已知基线错误阻塞。
- **策略版本展示**: 策略主名称恢复为基础名，不再把版本号写进 `策略名vN`；`#/workspace` 最近回测优化、`#/runs`、回测详情、策略详情与优化相关页面改为展示“策略名 + 版本标签”，其中回测与优化任务会优先使用各自持久化的历史版本信息，不再被当前最新版本覆盖。
- **优化配置版本一致性**: `#/optimization-jobs/new/config` 在无 `source_run_id` 的菜单入口下，参数范围“当前值”现在优先读取策略当前参数版本，不再被历史 `confirmation_fields` 旧值覆盖，避免页面显示高版本标签时仍沿用旧版本默认参数而造成版本判断错误。
- **旧任务约束清洗**: 旧版 Optimization Lab 任务在详情读取、结果页水合与重新过滤时会主动清洗 legacy `turnover` 约束，避免“约束条件”模块重新冒出已下线的第六项 `换手率`。
- **优化结果评分一致性**: Optimization Lab 结果页的“当前组合”基线行改为复用候选组合同一套稳定度与综合评分模型，不再出现当前组合显示稳定度 `88`、综合得分 `1.325`，而候选列表却按另一套分数与稳定度排序的口径不一致问题。
- **优化结果稳定度显示**: Optimization Lab 结果页加载“当前组合”基线行时不再只取 `view=metrics` 的轻量回测详情，避免缺少 `consistency_score` / `chart_series` 时把稳定度错误回退为 `0`。
- **优化配置**: `#/optimization-jobs/new/config` 的 `观察周期` 不再被错误建模为单值范围，现改为 `每日 / 每周 / 每月` 多选项，并同步支持后端按离散枚举展开优化组合。
- **动量调仓频率**: `#/optimization-jobs/new/config` 现支持将动量策略 `调仓频率` 作为 `每月 / 每季度 / 每半年 / 每年` 离散优化维度；候选晋升与复制版本时也会同步更新策略顶层 `rebalance_frequency`，保证策略详情页与新建-动量策略页展示一致。
- **优化结果计数**: 旧版只保存 `candidates_json`、未落 `optimization_job_trials` 的优化任务，现在会按已保存候选重算 `matching_combination_count`，避免出现“符合过滤条件的组合共 0 个”却仍显示“当前首选组合”的矛盾状态。
- **历史任务结果提示**: `#/optimization-jobs/:id` 遇到缺少全量 `optimization_job_trials` 的旧任务时，会明确标记“仅基于已保存候选”，不再把 4 个历史候选误说成 2880 组组合的完整筛选结果。
- **参数候选盘排序**: `#/optimization-jobs/:id` 读取完成态优化任务时，若历史 `candidates_json` 的首位候选已经落后于 `matching_combinations` 的真实头名，会自动基于持久化 `optimization_job_trials` 重建候选版本，避免参数候选盘第 1 名低于“查看全部组合”的实际 `年化收益率 Max`。
- **重新过滤列表闪动**: `#/optimization-jobs/:id` 点击“重新过滤”后，参数候选盘改为等待重新过滤结果返回后再统一刷新，不再先按页面本地状态重排、再跟随后端响应二次更新造成列表连续跳动。
- **多窗口验证**: 多窗口验证表现在会补充每个窗口的 `YYYY-MM-DD 至 YYYY-MM-DD` 周期说明，并修复窗口年化收益率错误显示为 `-` 的问题。

## [0.1.1-003] - 2026-04-16 - 新增离线价格补源与优化实验室结果兜底入口

### 新增 (Added)

- **数据源**: 引入离线 `Stooq` ZIP 价格源，可从 `GRIT_STOOQ_US_DAILY_ZIP` 或默认冷启动包补齐长历史日线。
- **交互**: Optimization Lab 结果页新增“一键放宽到有结果”入口，可按当前最佳候选自动回填阈值并立即重新过滤。

### 优化 (Changed)

- **策略列表性能**: 策略选择页改为复用 `latest_completed_run_summary` 摘要数据，不再逐条拉取完整回测详情，显著缩短首屏等待。
- **轮询链路**: Optimization Lab 进度轮询加入自适应退避、抖动、失焦暂停与恢复后即时拉取，减少无效请求并保留 `inFlight` 去重。
- **后端吞吐**: `get_optimization_job_detail` 在运行态加入 `job_id + updated_at` 的 1 秒短时复用，降低无变更窗口下的重复聚合查询。
- **可观测性**: `get_optimization_job_detail` 补充请求频率、平均耗时和 snapshot 命中率指标，便于量化提速方案的回归效果。
- **快照遥测**: `provider_summary` 现在会持久化到数据集 metadata，并将纯价格源排除在 corporate action provider summary 之外。

### 修复 (Fixed)

- **快照编号**: 修正 `pre-push` 补跑 `CHANGELOG.md` 时的快照编号与日期规则，避免补推送被错误记为 `-001`。
- **公司行为诊断**: 补充 `complete_no_events` probe coverage，消除无正式事件 symbol 被误判为 corporate missing 的假阴性。
- **快照修复**: repair 会按 `covered_symbols` 重算缺口与状态，避免陈旧 `missing_symbols` 让任务卡在历史假状态。
- **重新过滤**: 当前组合仍通过但候选全被快捷过滤挡住时不再无入口卡死；筛空结果时也不会错误落盘并切换本地结果态。
- **错误提示**: “无符合条件的组合” 统一改用顶部居中的 `error-banner` 展示，解决右下角私有样式带来的溢出问题。
- **护栏卡片**: 配置页将约束改写为“最大回撤 ≤ 25%”这类规则句，并移除卡片内重复的“当前判定”信息，提升两列布局可读性。
- **时间窗口继承**: 使用 `source_run_id` 创建优化任务时会继承原始 `request_json` 时间窗口，缺失窗口时直接报错，不再回退整段历史数据。
- **结果计数**: 终态详情缺少 `matching_combination_count` 时会直接报错并停止展示，避免把候选池数量误当成符合条件的组合总数。

## [0.1.1-002] - 2026-04-15 - 引入 Optimization Lab 并补强快照修复与历史数据链路

### 新增 (Added)

- **价格补洞**: 数据集快照新增 `yfinance` 回退提供方，用于 `Yahoo HTTP` 缺 bars、长历史缺口或公司行为补齐场景。
- **任务心跳**: 股票池阶段新增 heartbeat 与定向修复窗口，实时任务会显式经过 `selection -> latest_market_data -> finalizing -> universe_provider`。
- **参数优化**: 新增 Optimization Lab 模块，打通参数配置、结果展示、轮询刷新及对应页面测试。
- **策略类型**: 新增动量策略，贯通策略模板、回测指标、策略详情和运行引用链路。
- **数据覆盖**: 新增更完整的市场数据与股票池历史能力，接入官方指数公告及 FMP、Tiingo、Alpha Vantage、SEC EDGAR、AkShare US、Longbridge 等提供方。
- **历史回填**: 新增纳斯达克 100 的 Wikipedia 变更表回填能力，可在历史修订不足时基于 `2015` curated dataset 重建 `2007+` 锚点缺口。
- **旧版解析**: 新增纳斯达克 100 旧版 Wikipedia 列表解析器，支持 `2005-2007` 缺失 wikitable 的历史页面。
- **归档回退**: 新增 Internet Archive / 归档 Wikipedia 快照回填，用于补齐 2004 年以前的纳斯达克 100 历史锚点。
- **运行详情**: 新增总览、诊断、属性等视图，可直接查看执行证据、参数快照和优化上下文。
- **验证工具**: 新增 harness 文档、fixture 生成能力、smoke 脚本和技术操作指南，让本地重复验证成为标准流程。

### 优化 (Changed)

- **价格链路**: 数据集价格链调整为 `Yahoo HTTP -> yfinance -> Tiingo -> Longbridge -> AkShare -> FMP`，并把 `Alpha Vantage` 价格端收敛到 `repair` 缺口批次。
- **遥测落库**: 运行时 provider telemetry 新增 `yfinance` 选中与未选中成功统计，并将 `Alpha Vantage` 定点补价与普通价格链分开落库。
- **股票池免费链**: 默认顺序调整为 `Wikipedia 历史修订 + 官方公告 + GitHub curated dataset`，`FMP historical constituent` 降级为 entitlement 成功后的付费可选源。
- **历史修复范围**: 股票池修复改为只处理不完整的半年度锚点，不再每次重载过去 30 年全部 `01-01 / 07-01` 节点。
- **标普回填**: 标普 500 历史修复会预抓一个下游半年度锚点，便于基于更晚版本重建更早回退锚点。
- **刷新统计**: 股票池刷新统计新增历史锚点进展字段，可区分“成员行未变化”和“历史锚点质量提升”。
- **刷新稳定性**: 快照刷新与恢复逻辑整体加强，使官方来源回退、证券身份映射和股票池历史更新更稳定。
- **状态视觉**: 快照页 `READY/就绪` 标签调整为明确绿色成功态，完成状态更容易识别。
- **研究链路**: 工作台、快照页、运行列表、回测提交页与策略详情页的串联体验扩展到优化阶段。
- **续跑可见性**: 优化任务执行与续跑机制升级，在后端 API 与前端页面中持久化更多 trial 进度、heartbeat、ETA 和候选结果状态。
- **本地性能**: 本地运行时的回测与优化性能进一步提升，尤其改善 Windows 下的长时间任务体验。
- **文档同步**: 架构、技术、设计与 README 文档同步刷新，使工作流和运维脚本与当前实现保持一致。

### 修复 (Fixed)

- **快照编排**: 同步重建全部历史锚点时，`repair + universes` 不再长时间卡在 `preflight`。
- **过滤统计**: 当前组合满足快捷过滤条件时，结果页顶部“符合约束”统计会纳入当前组合，不再错误显示“暂无候选版本通过过滤”。
- **空结果态**: 结果页首屏会展示当前约束标签，候选表只保留满足约束的版本，零匹配任务会明确回退到空结果状态。
- **首屏按钮**: Optimization Lab 结果页与配置页的首屏按钮宽度和换行表现保持一致，不再挤成纵向堆叠。
- **首组启动**: 快照就绪预检迁移到 `dataset_symbol_coverage`，并让 `run-optimization` 延迟加载应用与提供方逻辑，显著缩短首组 trial 启动时间。
- **热路径读取**: 优化任务读取 `dataset_price_bars` 时不再为每根 bar 解码无关 `metadata_json`，显著缩短真实策略的首组开始与完成时间。
- **继续优化**: 手动“继续优化”后会立即返回 `RUNNING` 快照，并同步 `completed_combinations`、`persisted_trial_count` 与 `next_trial_index`。
- **价格额度**: `repair` 路径不再错误消耗 `Alpha Vantage` 免费价格额度，`incremental` 默认也不会触发 Alpha 全池尝试。
- **事实层语义**: 规范数据集快照继续保持原始 `OHLC + adj_close` 事实层，不再把 adjusted OHLC 或 forward fill 写入持久化快照。
- **刷新摘要**: 即使 `updated_row_count = 0`，只要历史锚点来源提升，快照页顶部也会显示历史进展摘要。
- **数据边界**: 多处快照与市场数据边界场景得到修复，降低刷新或 repair 后本地数据平面不完整或不一致的风险。
- **进度状态**: 运行中或被中断的优化任务现在能在 API 与 UI 中呈现更清晰且一致的状态。
- **验证链路**: 本地 smoke 与 live acceptance 的准备链路统一为 fixture 重置、测试入口和真实 API 校验脚本，提升重复验证稳定性。
- **动量参数**: 动量回测现在会真实使用 `skip_recent_months`、`hold_rank_threshold`、`weighting_method` 和 `rebalance_anchor_dates`，并把 `capital` 写入 `initial_equity`。
- **评分排序**: 优化评分不再被 `total_return_pct` 放大，改为以 `return_sharpe`、`out_of_sample_sharpe`、Calmar、稳定度、回撤和换手约束为主的风险调整排序。
- **候选重建**: 调整约束后，Optimization Lab 结果中心会基于完整 `optimization_job_trials` 重建候选，并优先返回满足当前约束的不同版本。

## [0.1.1] - 2026-04-01 - 补齐恢复后主链路页面并增强 Windows 本地启动体验

### 新增 (Added)

- 新增更完整的 Windows 快速启动流程，包含运行时校验、修复辅助脚本和更稳妥的本地启动默认项。
- 新增基于路由的 React 应用入口，以及工作台、策略创建、回测提交、运行详情、运行列表、快照页、策略详情等一组正式页面。
- 新增 shell 框架、中文运行时入口，以及覆盖恢复主链路的更多前后端聚焦测试。
- 新增快照恢复、回退提供方支持和 live acceptance 辅助脚本，用于在本地数据上验证恢复后的主链路。
- 新增恢复方案文档与设计稿沉淀，用于记录重建后的产品方向。

### 优化 (Changed)

- 将恢复后的平台从初始基线扩展为更完整的 `workspace -> creation -> backtest -> run detail -> snapshots` 主链路体验。
- 改进恢复态后端服务、存储层、市场数据访问和 API 契约，让基于本地 SQLite 的流程更接近可用产品，而不是局部修复快照。
- 完成围绕 `app-runtime` 路由、共享适配层和页面级测试覆盖的前端架构收口。
- 更新 README 与架构文档，使其与真实路由图、运行约束和启动方式一致。

### 修复 (Fixed)

- 修复多处本地环境阻塞问题，包括 GitHub 推送辅助链路、凭证受阻场景和快速启动健康检查。
- 修复策略创建、运行详情、工作台和快照相关视图中的多项恢复回归问题，使项目恢复度提升至约 80%。

## [0.1.0] - 2026-03-30 - 重建恢复后的首个可运行基线并重新接通本地前后端工作台

### 新增 (Added)

- 新增 Grit Strategy Lab 在 2026 年 3 月恢复工作后的首个可运行基线，重新建立 FastAPI 后端与 React/Vite 前端工作台。
- 新增基线恢复提交中提到的生命周期清理、交易审计能力和运行时自愈机制。
- 新增本地 SQLite 种子数据库、打包后的 Python 运行时资源，以及在 Windows 上启动重建平台所需的初始工程结构。
