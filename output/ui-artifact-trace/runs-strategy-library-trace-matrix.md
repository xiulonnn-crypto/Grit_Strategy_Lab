# Runs Strategy Library Trace Matrix

Scope: `#/runs` backtest history aggregation. Implementation is split across `web/src/pages/runs-index-page.tsx`, `web/src/pages/runs-index-page.css`, `web/src/lib/runs-strategy-library-view-model.ts`, and the focused route/page tests.

| Design / spec requirement | Expected selector or role | Owned verification | Notes |
| --- | --- | --- | --- |
| Route owns a stable page root and no longer treats a table as the primary route proof. | `[data-page-root="runs-index"]` or `.runs-index-page` | `web/src/app.routes.foundation.test.tsx` route test | Route assertion now requires page/root plus strategy-library evidence, not `findByRole('table')`. |
| Default surface is strategy library view with a visible recent-runs tab. | `role="tab"` names `策略库视图` and `最近运行`; active tab has `.is-active` | `web/src/runs.index.page.test.tsx` first test; `web/src/app.routes.foundation.test.tsx` route test | The route should start in strategy-library mode while keeping recent runs reachable. |
| Approved heading and toolbar match the reference artifact. | `.runs-page-heading`, `.runs-toolbar`, `role="tab"` names `策略库视图` and `最近运行`, search label `搜索策略、版本或 run id` | `web/src/runs.index.page.test.tsx` first test; live CDP screenshot | The route-level shell heading remains disabled; the runs page owns the compact heading metrics and toolbar. |
| Strategy-version evidence tree renders strategy, parameter version, and run evidence together. | `.runs-library-panel .runs-tree` containing strategy names, version tags, strategy summary metadata, and the latest expanded run IDs | `web/src/runs.index.page.test.tsx` first test; `web/src/app.routes.foundation.test.tsx` route test | The default expansion opens the first strategy and latest version; older version runs appear after drilling into that version. |
| First column matches the approved information architecture. | `.tree-meta`, `.badge-row`, `.version-title`, `.version-copy`, `.run-line`, `.run-meta` | `web/src/runs.index.page.test.tsx` first test; live CDP DOM scan | Strategy rows show run/version/current/best evidence summary; version rows hide parameter-version IDs; run rows show `周期 · 类型 · 日期范围`. |
| Smart taskbar exposes batch recovery work without destructive controls. | Heading `智能任务栏`, containing section `.runs-rail-section`, button name matching `批量补齐` | `web/src/runs.index.page.test.tsx` first and second tests; live CDP screenshot | Taskbar should summarize missing long-horizon work and trigger backfill. |
| Missing long-cycle evidence appears as virtual queue rows. | Virtual run content displays `排队中 / 计算中...`, with `排队中` and `计算中...` visible in row/task state | `web/src/runs.index.page.test.tsx` second test | These rows are UI projections inserted while the batch call is pending, not persisted completed runs. |
| Batch backfill submits missing long-cycle work from the runs page. | `role="button"` name matching `批量补齐`; `submitBacktestRun(strategyId, payload)` includes `parameter_version_id`, `source_run_id`, and `is_permanent: true` | `web/src/runs.index.page.test.tsx` second test | Worker D expects the existing submit API unless Lead introduces a dedicated bulk endpoint. |
| Recent-runs tab restores the historical list with operations. | Section `aria-label="最近运行"`, heading `最近运行时间线`, columnheader `操作`, row buttons `删除` | `web/src/runs.index.page.test.tsx` third and fourth tests; live CDP recent screenshot | Selecting a run updates diagnostics in-place, while deletion stays behind a second confirmation dialog. |
| Strategy-library tab remains read-only and does not expose compare flow. | No `role="checkbox"`; no text `对比模式`; no `.runs-compare-mode` / `[data-testid="runs-compare-mode"]`; no `删除` button inside `.runs-library-panel` | `web/src/runs.index.page.test.tsx` fourth test | Destructive operation is available only in the historical recent-runs list. |
| Column alignment stays semantic after table removal. | `.runs-evidence-grid--header`, `.runs-evidence-cell--number`, `.runs-recent-grid--header` | `web/src/runs.index.page.test.tsx`; `web/src/app.routes.foundation.test.tsx`; live CDP geometry scan | Browser evidence checks right-edge / center alignment with `maxAlignedDelta: 0` and verifies the evidence board fits the shell at 1440px. |
| Status semantics follow the approved financial evidence states. | Status chips containing `路径成熟`, `存在漂移`, `证据断裂`; view model status values `MATURE`, `DRIFT`, `BROKEN`, `FAILED_REVIEW` | `web/src/runs-strategy-library-view-model.test.ts`; `web/src/runs.index.page.test.tsx` | Missing 20Y/30Y is treated as evidence break, while drift requires completed long-cycle evidence with warning or Sharpe spread. |
